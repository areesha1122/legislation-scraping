export const regex_english = /^[a-zA-Z]+$/;
export const regex_number = /^(?:[1-9]\d*|\d)$/;
// Regex to find a four-digit year
export const regex_year = /\b\d{4}\b/;
export const date_regex = /as at (\d{1,2} \w+ \d{4})/;
